###
# Cluster API
###

kind: Deployment
apiVersion: apps/v1
metadata:
  labels:
    cluster: application
    component: app-cluster-api
  name: app-cluster-api
  namespace: __NPH_NAMESPACE
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      cluster: application
      component: app-cluster-api
  template:
    metadata:
      labels:
        cluster: application
        component: app-cluster-api
    spec:
      containers:
      - name: app-cluster-api
        image: __NPH_REGISTRY/__NPH_REGISTRY_NAMESPACE/app-cluster-api:__NPH_VERSION
        imagePullPolicy: Always
        args:
        - "run"
        securityContext:
          runAsUser: 2000
      imagePullSecrets:
      - name: nalej-registry
